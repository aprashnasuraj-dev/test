# Child process execution uses shell mode or a constructed command string.

**Severity:** Medium
**Asset:** explorer
**Impact:** Impact requires reviewer confirmation from the supplied trace.

## Summary
Child process execution uses shell mode or a constructed command string.

## Vulnerability Detail
Child process execution uses shell mode or a constructed command string.

- Root cause: `home.runner.work.test.test.ens-audit-runner.src.ens_audit.rules.ens-shell-true-or-interpolated-command`
- Rule: `home.runner.work.test.test.ens-audit-runner.src.ens_audit.rules.ens-shell-true-or-interpolated-command`
- Location: `/home/runner/ens-audit/repos/audit-comp-ens/apps/portal/src/worker/og-render.ts:15`

## Impact
Impact requires reviewer confirmation from the supplied trace.

## Proof of Concept
```text
Child process execution uses shell mode or a constructed command string.
```

## Recommendation
Add a fail-closed validation at the identified trust boundary and add a regression test.

## References
- None recorded
